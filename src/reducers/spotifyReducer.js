export const intialState = {
    username: "",
    isFethcingData: false,
    errorMessage: "",

}





export const spotifyReducer = (state = intialState, action) => {
    switch(action.type) {
        default: 
            return state;
    }
}